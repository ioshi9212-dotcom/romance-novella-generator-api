name: Debug bootstrap source fidelity

on:
  push:
    branches:
      - agent/enforce-bootstrap-source-fidelity
    paths:
      - .github/workflows/debug-bootstrap-source-fidelity.yml
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/debug-bootstrap-source-fidelity.yml

permissions:
  contents: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/enforce-bootstrap-source-fidelity
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install test dependencies
        run: |
          python -m pip install -r requirements.txt
          python -m pip install pytest==8.3.4 httpx==0.28.1
      - name: Capture focused test output
        run: |
          set +e
          python -m pytest tests/test_bootstrap_source_fidelity.py -q --maxfail=5 > ci-bootstrap-source-fidelity.txt 2>&1
          code=$?
          echo "" >> ci-bootstrap-source-fidelity.txt
          echo "PYTEST_EXIT_CODE=$code" >> ci-bootstrap-source-fidelity.txt
          exit 0
      - name: Commit diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ci-bootstrap-source-fidelity.txt
          git commit -m "Capture bootstrap source fidelity failure"
          git push origin HEAD:agent/enforce-bootstrap-source-fidelity
